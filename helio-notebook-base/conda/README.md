# about

The basic environment we need to run on the cloud. Since we inherit currently from pangeo, we need to match the
non-earth science libraries in pangeo-notebook here in order to be compatible.
